% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_extract.R
\name{vcov.poisselect}
\alias{vcov.poisselect}
\title{Extract the Covariance Matrix of a Poisson Selection Model}
\usage{
\method{vcov}{poisselect}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"poisselect"}.}

\item{...}{Currently ignored, present for consistency with the generic.}
}
\value{
A numeric matrix with one row and column per parameter.
}
\description{
The matrix refers to the original parametrisation, that is to \code{sigma} and
\code{rho} rather than to \code{log(sigma)} and \code{atanh(rho)}, and covers all
parameters of both equations. Its row and column names match
\code{names(coef(object))}.
}
\examples{
fit <- poisselect(y ~ x1 + x2, s ~ x1 + z1, data = sim_selection)
round(vcov(fit), 4)
# The standard errors are the square roots of the diagonal.
sqrt(diag(vcov(fit)))

}
